# AIRL vs Python Benchmark Results

**Date:** 2026-03-21 19:01:55
**Tasks:** 25

## Hypothesis

1. **Token Efficiency:** AIRL programs are more concise than equivalent Python with contracts.
2. **Correctness:** AI-generated AIRL compiles and runs correctly as often as Python.
3. **Contract Safety:** AIRL's mandatory contracts catch errors that Python asserts miss.
4. **Intent Recoverability:** An AI can reconstruct the original specification more accurately from AIRL than from Python.

## Results

| Task | AIRL chars | AIRL words | AIRL lines | Python chars | Python words | Python lines | AIRL runs | Python runs | AIRL intent | Python intent |
|------|-----------|-----------|-----------|-------------|-------------|-------------|-----------|-------------|-------------|---------------|
| Safe Divide        |       493 |        76 |        12 |         658 |          97 |          24 | FAIL      | PASS        | 4.5         | 4.8           |
| Fibonacci          |       232 |        42 |        10 |         462 |          86 |          22 | PASS      | PASS        | 4.5         | 5.0           |
| List Processing    |       286 |        47 |        12 |         989 |         151 |          23 | PASS      | FAIL        | 5.0         | 5.0           |
| Input Validation   |       371 |        49 |        14 |        1220 |         175 |          38 | PASS      | PASS        | 5.0         | 4.5           |
| String Tokenizer   |       222 |        34 |         9 |         432 |          59 |          15 | PASS      | PASS        | 4.5         | 5.0           |
| Absolute Value     |       161 |        27 |         7 |         436 |          56 |          18 | PASS      | PASS        | 19.5        | 4.5           |
| GCD                |       205 |        45 |        10 |         646 |         123 |          23 | PASS      | PASS        | 4.8         | 5.0           |
| Power              |       257 |        46 |        11 |         636 |          97 |          25 | PASS      | PASS        | 5.0         | 4.8           |
| Reverse List       |       207 |        34 |         9 |         698 |         100 |          24 | PASS      | PASS        | 4.5         | 4.5           |
| Find Max           |       288 |        55 |        11 |         741 |         123 |          26 | PASS      | FAIL        | 4.5         | 5.0           |
| Remove Duplicates  |       738 |        96 |        26 |         987 |         130 |          30 | PASS      | PASS        | 5.0         | 4.8           |
| Zip Lists          |       334 |        52 |        13 |         791 |         112 |          24 | PASS      | PASS        | 5.0         | 5.0           |
| Palindrome Check   |       286 |        39 |        10 |         513 |          65 |          12 | PASS      | PASS        | 5.0         | 4.8           |
| Count Vowels       |       501 |        75 |        20 |         384 |          52 |          14 | PASS      | PASS        | 5.0         | 4.8           |
| Caesar Cipher      |       861 |       121 |        27 |         826 |         111 |          26 | PASS      | FAIL        | 5.0         | 5.0           |
| Safe Sqrt          |       482 |        77 |        21 |         863 |         132 |          34 | PASS      | PASS        | 4.5         | 5.0           |
| Parse Int          |      1375 |       210 |        61 |         704 |          96 |          28 | PASS      | PASS        | 5.0         | 4.8           |
| Bounded Access     |       244 |        40 |        11 |         785 |         115 |          27 | PASS      | FAIL        | 4.5         | 4.8           |
| Flatten List       |       216 |        36 |         9 |         591 |          92 |          19 | PASS      | FAIL        | 4.8         | 5.0           |
| Group by Parity    |       394 |        67 |        14 |         958 |         177 |          23 | PASS      | PASS        | 4.8         | 4.5           |
| Running Sum        |       445 |        56 |        15 |         673 |          99 |          24 | PASS      | PASS        | 4.2         | 5.0           |
| Word Frequency     |       876 |       108 |        23 |         892 |         122 |          30 | PASS      | PASS        | 4.8         | 5.0           |
| Matrix Transpose   |       466 |        70 |        16 |        1069 |         161 |          30 | PASS      | FAIL        | 4.5         | 5.0           |
| Merge Sorted       |       494 |        77 |        17 |        1262 |         194 |          41 | PASS      | FAIL        | 4.5         | 4.5           |
| Pipeline           |       334 |        56 |        11 |         620 |          98 |          20 | PASS      | FAIL        | 5.0         | 4.8           |

## Totals

- **AIRL correct:** 24 / 25
- **Python correct:** 17 / 25
- **Total AIRL chars:** 10768
- **Total Python chars:** 18836
- **Char ratio (AIRL/Python):** 0.57

## Generated Code

### AIRL Programs

#### Task 01: Safe Divide

```lisp
(defn safe-divide
  :sig [(a : i64) (b : i64) -> Result]
  :requires [(valid a)]
  :ensures [(valid result)]
  :body (if (= b 0)
          (Err "division by zero")
          (Ok (/ a b))))

(print (safe-divide 10 3))
(print (safe-divide 10 0))

This defines `safe-divide` returning `(Ok 3)` for `(safe-divide 10 3)` and `(Err "division by zero")` for `(safe-divide 10 0)`. The `Result` type with `Ok`/`Err` variants prevents crashes by encoding failure as a value rather than a runtime error.
```

**Exit code:** 1
**Output:** `error: unexpected character: '`'
  --> /mnt/b6d8b397-9fc1-42ac-a0da-8664a73d4ee9/AIRL/benchmarks/output/airl/01.airl:12:13
   |
 12 | This defines `safe-divide` returning `(Ok 3)` for `(safe-divide 10 3)` and `(Err "division by zero")` for `(safe-divide 10 0)`. The `Result` type with `Ok`/`Err` variants prevents crashes by encoding failure as a value rather than a runtime error.
   |              ^`

#### Task 02: Fibonacci

```lisp
(defn fibonacci
  :sig [(n : i64) -> i64]
  :requires [(>= n 0) (<= n 30)]
  :ensures [(>= result 0)]
  :body (if (= n 0) 0
          (if (= n 1) 1
            (+ (fibonacci (- n 1)) (fibonacci (- n 2))))))

(print (fibonacci 10))
```

**Exit code:** 0
**Output:** `warning: undefined symbol: `=`
warning: undefined symbol: `fibonacci`
warning: contract disproven in `fibonacci`: (>= result 0) (counterexample: [("n", "0")])
55`

#### Task 03: List Processing

```lisp
(defn sum-doubled-evens
  :sig [(xs : List) -> i64]
  :requires [(valid xs)]
  :ensures [(>= result 0)]
  :body
    (fold (fn [acc x] (+ acc x))
          0
          (map (fn [x] (* x 2))
               (filter (fn [x] (= (% x 2) 0)) xs))))

(print (sum-doubled-evens [1 2 3 4 5 6]))
```

**Exit code:** 0
**Output:** `warning: undefined symbol: `sum-doubled-evens`
24`

#### Task 04: Input Validation

```lisp
(defn validate-age
  :sig [(age : i64) -> Result[i64, String]]
  :requires [(valid age)]
  :ensures [(valid result)]
  :body (if (and (>= age 0) (<= age 150))
          (Ok age)
          (if (< age 0)
            (Err "Age cannot be negative")
            (Err "Age cannot exceed 150"))))

(print (validate-age 25))
(print (validate-age -5))
(print (validate-age 200))
```

**Exit code:** 0
**Output:** `warning: if branches have different types: Named { name: "Ok", args: [Type(Prim(I64))] } vs Named { name: "Err", args: [Type(Prim(Str))] }
warning: undefined symbol: `validate-age`
warning: undefined symbol: `validate-age`
warning: undefined symbol: `validate-age`
(Ok 25)
(Err "Age cannot be negative")
(Err "Age cannot exceed 150")`

#### Task 05: String Tokenizer

```lisp
(defn tokenize
  :sig [(s : String) -> List]
  :requires [(valid s)]
  :ensures [(valid result)]
  :body (filter (fn [tok] (> (length tok) 0)) (split s " ")))

(print (tokenize "hello  world  foo"))
(print (tokenize ""))
```

**Exit code:** 0
**Output:** `warning: undefined symbol: `tokenize`
warning: undefined symbol: `tokenize`
["hello" "world" "foo"]
[]`

#### Task 06: Absolute Value

```lisp
(defn absolute-value
  :sig [(x : i64) -> i64]
  :requires [(valid x)]
  :ensures [(>= result 0)]
  :body (if (< x 0) (* x -1) x))

(print (absolute-value -42))
```

**Exit code:** 0
**Output:** `warning: contract disproven in `absolute-value`: (>= result 0) (counterexample: [("x", "0")])
42`

#### Task 07: GCD

```lisp
(defn gcd
  :sig [(a : i64) (b : i64) -> i64]
  :requires [(>= a 0) (>= b 0)]
  :ensures [(>= result 0)]
  :body (if (= b 0) a (gcd b (% a b))))

(print (gcd 12 8))
(print (gcd 15 0))
(print (gcd 48 18))
```

**Exit code:** 0
**Output:** `warning: undefined symbol: `=`
warning: undefined symbol: `gcd`
warning: undefined symbol: `gcd`
warning: undefined symbol: `gcd`
warning: contract disproven in `gcd`: (>= result 0) (counterexample: [("a", "0"), ("b", "0")])
4
15
6`

#### Task 08: Power

```lisp
(defn power
  :sig [(base : i64) (exp : i64) -> i64]
  :requires [(>= exp 0)]
  :ensures [(if (> base 0) (>= result 1) (valid result))]
  :body (if (= exp 0)
          1
          (* base (power base (- exp 1)))))

(print (power 2 10))
(print (power 3 0))
```

**Exit code:** 0
**Output:** `warning: undefined symbol: `=`
warning: undefined symbol: `power`
warning: undefined symbol: `power`
1024
1`

#### Task 09: Reverse List

```lisp
(defn reverse-list
  :sig [(xs : List) -> List]
  :requires [(valid xs)]
  :ensures [(= (length result) (length xs))]
  :body
    (fold (fn [acc x] (cons x acc)) [] xs))

(print (reverse-list [1 2 3 4 5]))
```

**Exit code:** 0
**Output:** `warning: undefined symbol: `reverse-list`
[5 4 3 2 1]`

#### Task 10: Find Max

```lisp
(defn find-max
  :sig [(xs : List) -> i64]
  :requires [(not (empty? xs))]
  :ensures [(all (fn [x] (>= result x)) xs)]
  :body
    (fold (fn [acc x] (if (> x acc) x acc)) (head xs) (tail xs)))

(print (find-max [3 1 4 1 5 9]))
(print (find-max [42]))
(print (find-max [3 7 2 8 1 9 4]))
```

**Exit code:** 0
**Output:** `warning: undefined symbol: `find-max`
warning: undefined symbol: `find-max`
warning: undefined symbol: `find-max`
9
42
9`

#### Task 11: Remove Duplicates

```lisp
(defn member?
  :sig [(x : i64) (xs : List) -> bool]
  :ensures [(valid result)]
  :body (if (empty? xs)
          false
          (if (= x (head xs))
            true
            (member? x (tail xs)))))

(defn remove-duplicates-helper
  :sig [(xs : List) (seen : List) -> List]
  :ensures [(valid result)]
  :body (if (empty? xs)
          (reverse seen)
          (let (h : i64 (head xs))
            (if (member? h seen)
              (remove-duplicates-helper (tail xs) seen)
              (remove-duplicates-helper (tail xs) (cons h seen))))))

(defn remove-duplicates
  :sig [(xs : List) -> List]
  :ensures [(<= (length result) (length xs))]
  :body (remove-duplicates-helper xs []))

(print (remove-duplicates [5 3 5 1 3 2 1]))
```

**Exit code:** 0
**Output:** `warning: undefined symbol: `remove-duplicates`
[5 3 1 2]`

#### Task 12: Zip Lists

```lisp
(defn zip-lists
  :sig [(xs : List) (ys : List) -> List]
  :requires [(valid xs) (valid ys)]
  :ensures [(valid result)]
  :body
    (if (or (empty? xs) (empty? ys))
      []
      (cons [(head xs) (head ys)]
            (zip-lists (tail xs) (tail ys)))))

(print (zip-lists [1 2 3] [10 20 30]))
(print (zip-lists [1 2] [10 20 30]))
```

**Exit code:** 0
**Output:** `warning: undefined symbol: `zip-lists`
warning: undefined symbol: `zip-lists`
[[1 10] [2 20] [3 30]]
[[1 10] [2 20]]`

#### Task 13: Palindrome Check

```lisp
(defn is-palindrome
  :sig [(s : String) -> bool]
  :requires [(valid s)]
  :ensures [(or (= result true) (= result false))]
  :body (let (cs : List (chars s))
          (= cs (reverse cs))))

(print (is-palindrome "racecar"))
(print (is-palindrome "hello"))
(print (is-palindrome ""))
```

**Exit code:** 0
**Output:** `warning: undefined symbol: `is-palindrome`
warning: undefined symbol: `is-palindrome`
warning: undefined symbol: `is-palindrome`
true
false
true`

#### Task 14: Count Vowels

```lisp
(defn count-vowels
  :sig [(s : String) -> i64]
  :requires [(valid s)]
  :ensures [(>= result 0) (<= result (length s))]
  :body
  (let (lower : String (to-lower s))
    (let (cs : List (chars lower))
      (fold (fn [acc c]
        (if (or (= c "a") (= c "e"))
          (+ acc 1)
          (if (or (= c "i") (= c "o"))
            (+ acc 1)
            (if (= c "u")
              (+ acc 1)
              acc))))
        0 cs))))

(print (count-vowels "hello world"))
(print (count-vowels "xyz"))
```

**Exit code:** 0
**Output:** `warning: undefined symbol: `count-vowels`
warning: undefined symbol: `count-vowels`
3
0`

#### Task 15: Caesar Cipher

```lisp
(defn shift-char
  :sig [(c : String) (shift : i64) -> String]
  :requires [(>= shift 0) (<= shift 25)]
  :ensures [(valid result)]
  :body
  (let (lower : String "abcdefghijklmnopqrstuvwxyz")
    (let (upper : String "ABCDEFGHIJKLMNOPQRSTUVWXYZ")
      (let (li : i64 (index-of lower c))
        (if (!= li -1)
          (char-at lower (% (+ li shift) 26))
          (let (ui : i64 (index-of upper c))
            (if (!= ui -1)
              (char-at upper (% (+ ui shift) 26))
              c)))))))

(defn caesar-cipher
  :sig [(s : String) (shift : i64) -> String]
  :requires [(>= shift 0) (<= shift 25)]
  :ensures [(= (length result) (length s))]
  :body
  (let (char-list : List (chars s))
    (join (map (fn [c] (shift-char c shift)) char-list) "")))

(print (caesar-cipher "abc" 3))
(print (caesar-cipher "xyz" 3))
(print (caesar-cipher "hello" 3))
```

**Exit code:** 0
**Output:** `warning: if branches have different types: TypeVar("_") vs Prim(Str)
warning: undefined symbol: `caesar-cipher`
warning: undefined symbol: `caesar-cipher`
warning: undefined symbol: `caesar-cipher`
"def"
"abc"
"khoor"`

#### Task 16: Safe Sqrt

```lisp
(defn isqrt-helper
  :sig [(x : i64) (n : i64) -> i64]
  :requires [(>= x 0) (>= n 0)]
  :ensures [(>= result 0)]
  :body (if (> (* (+ n 1) (+ n 1)) x)
          n
          (isqrt-helper x (+ n 1))))

(defn safe-sqrt
  :sig [(x : i64) -> Result]
  :requires [(valid x)]
  :ensures [(valid result)]
  :body (if (< x 0)
          (Err "negative input")
          (Ok (isqrt-helper x 0))))

(print (safe-sqrt 16))
(print (safe-sqrt 10))
(print (safe-sqrt -1))
(print (safe-sqrt 25))
```

**Exit code:** 0
**Output:** `warning: undefined symbol: `isqrt-helper`
warning: undefined symbol: `safe-sqrt`
warning: undefined symbol: `safe-sqrt`
warning: undefined symbol: `safe-sqrt`
warning: undefined symbol: `safe-sqrt`
warning: contract disproven in `isqrt-helper`: (>= result 0) (counterexample: [("x", "0"), ("n", "0")])
(Ok 4)
(Ok 3)
(Err "negative input")
(Ok 5)`

#### Task 17: Parse Int

```lisp
(defn digit-value
  :sig [(c : String) -> i64]
  :requires [(valid c)]
  :ensures [(>= result -1)]
  :body
  (if (= c "0") 0
  (if (= c "1") 1
  (if (= c "2") 2
  (if (= c "3") 3
  (if (= c "4") 4
  (if (= c "5") 5
  (if (= c "6") 6
  (if (= c "7") 7
  (if (= c "8") 8
  (if (= c "9") 9
  -1)))))))))))

(defn all-digits?
  :sig [(cs : List) -> bool]
  :requires [(valid cs)]
  :ensures [(valid result)]
  :body
  (if (empty? cs)
    true
    (if (< (digit-value (head cs)) 0)
      false
      (all-digits? (tail cs)))))

(defn chars-to-int
  :sig [(cs : List) (acc : i64) -> i64]
  :requires [(valid cs)]
  :ensures [(valid result)]
  :body
  (if (empty? cs)
    acc
    (chars-to-int (tail cs) (+ (* acc 10) (digit-value (head cs))))))

(defn parse-int
  :sig [(s : String) -> Result]
  :requires [(valid s)]
  :ensures [(valid result)]
  :body
  (let (cs : List (chars s))
    (if (empty? cs)
      (Err "not a number")
      (if (= (head cs) "-")
        (let (rest : List (tail cs))
          (if (empty? rest)
            (Err "not a number")
            (if (all-digits? rest)
              (Ok (* -1 (chars-to-int rest 0)))
              (Err "not a number"))))
        (if (all-digits? cs)
          (Ok (chars-to-int cs 0))
          (Err "not a number"))))))

(print (parse-int "42"))
(print (parse-int "-7"))
(print (parse-int "abc"))
(print (parse-int "123"))
```

**Exit code:** 0
**Output:** `warning: undefined symbol: `=`
warning: undefined symbol: `parse-int`
warning: undefined symbol: `parse-int`
warning: undefined symbol: `parse-int`
warning: undefined symbol: `parse-int`
(Ok 42)
(Ok -7)
(Err "not a number")
(Ok 123)`

#### Task 18: Bounded Access

```lisp
(defn bounded-access
  :sig [(xs : List) (i : i64) -> Result]
  :requires [(>= i 0)]
  :ensures [(valid result)]
  :body
    (if (< i (length xs))
      (Ok (at xs i))
      (Err "out of bounds")))

(print (bounded-access [10 20 30 40 50] 2))
```

**Exit code:** 0
**Output:** `warning: undefined symbol: `bounded-access`
(Ok 30)`

#### Task 19: Flatten List

```lisp
(defn my-flatten
  :sig [(xss : List) -> List]
  :requires [(valid xss)]
  :ensures [(valid result)]
  :body (fold (fn [acc xs] (concat acc xs)) [] xss))

(let (r : List (my-flatten [[1 2] [3 4] [5]]))
  (print r))
```

**Exit code:** 0
**Output:** `warning: undefined symbol: `my-flatten`
[1 2 3 4 5]`

#### Task 20: Group by Parity

```lisp
(defn group-by-parity
  :sig [(xs : List) -> List]
  :requires [(valid xs)]
  :ensures [(= (length result) 2)]
  :body
    (let (evens : List (filter (fn [x] (= (% x 2) 0)) xs))
      (let (odds : List (filter (fn [x] (not (= (% x 2) 0))) xs))
        [evens odds])))

(let (res : List (group-by-parity [10 15 20 25 30]))
  (do
    (print "evens:" (at res 0))
    (print "odds:" (at res 1))))
```

**Exit code:** 0
**Output:** `warning: undefined symbol: `group-by-parity`
"evens:" [10 20 30]
"odds:" [15 25]`

#### Task 21: Running Sum

```lisp
(defn running-sum
  :sig [(xs : List) -> List]
  :requires [(valid xs)]
  :ensures [(= (length result) (length xs))]
  :body
    (let (helper : fn
           (fn [remaining acc total]
             (if (empty? remaining)
               acc
               (let (new-total : i64 (+ total (head remaining)))
                 (helper (tail remaining) (append acc new-total) new-total)))))
      (helper xs [] 0)))

(print (running-sum [1 2 3 4 5]))
```

**Exit code:** 0
**Output:** `warning: undefined symbol: `running-sum`
[1 3 6 10 15]`

#### Task 22: Word Frequency

```lisp
;; word-frequency: count occurrences of each word, preserving first-occurrence order

(defn word-frequency
  :sig [(s : String) -> List]
  :requires [(valid s)]
  :ensures [(valid result)]
  :body
    (let (words : List (split s " "))
      (let (state : List
             (fold (fn [acc w]
                     (let (counts : _ (at acc 0))
                       (let (order : List (at acc 1))
                         (if (map-has counts w)
                           [(map-set counts w (+ (map-get counts w) 1)) order]
                           [(map-set counts w 1) (append order w)]))))
                   [(map-new) []]
                   words))
        (let (final-counts : _ (at state 0))
          (let (final-order : List (at state 1))
            (map (fn [w] [w (map-get final-counts w)]) final-order))))))

(print (word-frequency "one two one three two one"))
```

**Exit code:** 0
**Output:** `warning: undefined symbol: `word-frequency`
[["one" 3] ["two" 2] ["three" 1]]`

#### Task 23: Matrix Transpose

```lisp
(defn get-column
  :sig [(matrix : List) (col : i64) -> List]
  :requires [(>= col 0)]
  :ensures [(valid result)]
  :body (map (fn [row] (at row col)) matrix))

(defn transpose
  :sig [(matrix : List) -> List]
  :requires [(not (empty? matrix))]
  :ensures [(= (length result) (length (head matrix)))]
  :body
    (let (num-cols : i64 (length (head matrix)))
      (map (fn [c] (get-column matrix c)) (range 0 num-cols))))

(print (transpose [[1 2] [3 4] [5 6]]))
```

**Exit code:** 0
**Output:** `warning: undefined symbol: `transpose`
[[1 3 5] [2 4 6]]`

#### Task 24: Merge Sorted

```lisp
(defn merge-sorted
  :sig [(xs : List) (ys : List) -> List]
  :requires [(valid xs) (valid ys)]
  :ensures [(= (length result) (+ (length xs) (length ys)))]
  :body
    (if (empty? xs)
      ys
      (if (empty? ys)
        xs
        (if (<= (head xs) (head ys))
          (cons (head xs) (merge-sorted (tail xs) ys))
          (cons (head ys) (merge-sorted xs (tail ys)))))))

(print (merge-sorted [1 3 5] [2 4 6]))
(print (merge-sorted [] [1 2]))
(print (merge-sorted [1 3 5 7] [2 4 6 8]))
```

**Exit code:** 0
**Output:** `warning: undefined symbol: `merge-sorted`
warning: undefined symbol: `merge-sorted`
warning: undefined symbol: `merge-sorted`
[1 2 3 4 5 6]
[1 2]
[1 2 3 4 5 6 7 8]`

#### Task 25: Pipeline

```lisp
(defn positive-square-sum
  :sig [(xs : List) -> i64]
  :requires [(valid xs)]
  :ensures [(>= result 0)]
  :body
    (let (positives : List (filter (fn [x] (> x 0)) xs))
      (let (squares : List (map (fn [x] (* x x)) positives))
        (fold (fn [acc x] (+ acc x)) 0 squares))))

(print (positive-square-sum [3 -1 4 -1 5 -9 2]))
```

**Exit code:** 0
**Output:** `warning: undefined symbol: `positive-square-sum`
54`

### Python Programs

#### Task 01: Safe Divide

```python
def safe_divide(a, b):
    assert isinstance(a, int), "a must be an integer"
    assert isinstance(b, int), "b must be an integer"

    if b == 0:
        return ("Err", "division by zero")

    result = a // b
    return ("Ok", result)


# Test case 1: normal division
result = safe_divide(10, 3)
assert result == ("Ok", 3), f"Expected ('Ok', 3), got {result}"

# Test case 2: division by zero returns error, not crash
err_result = safe_divide(10, 0)
assert err_result[0] == "Err", f"Expected Err variant, got {err_result}"

# Postcondition: Ok results must contain an int
assert isinstance(result[1], int), "quotient must be an integer"

print(result[1])
```

**Exit code:** 0
**Output:** `3`

#### Task 02: Fibonacci

```python
def fibonacci(n):
    assert isinstance(n, int), "n must be an integer"
    assert n >= 0, "n must be non-negative"
    assert n <= 30, "n must be no greater than 30"

    if n == 0:
        return 0
    if n == 1:
        return 1

    a, b = 0, 1
    for _ in range(2, n + 1):
        a, b = b, a + b

    assert b > 0, "result must be positive for n >= 1"
    return b


result = fibonacci(10)
assert result == 55, f"Expected 55, got {result}"
print(result)
```

**Exit code:** 0
**Output:** `55`

#### Task 03: List Processing

```python
def sum_doubled_evens(xs):
    assert isinstance(xs, list), "input must be a list"
    assert all(isinstance(x, int) for x in xs), "all elements must be integers"

    evens = [x for x in xs if x % 2 == 0]
    doubled = [x * 2 for x in evens]
    result = sum(doubled)

    all_non_negative = all(x >= 0 for x in xs)
    if all_non_negative:
        assert result >= 0, "result must be non-negative when all inputs are non-negative"

    return result


assert sum_doubled_evens([]) == 0, "empty list must return 0"
assert sum_doubled_evens([1, 3, 5]) == 0, "no evens must return 0"
assert sum_doubled_evens([1, 2, 3, 4, 5, 6]) == 24, "expected 24"

result = sum_doubled_evens([1, 2, 3, 4, 5, 6])
print(result)

The function uses the filter-map-reduce pipeline: filter evens, double each, sum. Preconditions validate input types, and the postcondition ensures non-negative results for non-negative inputs. The inline assertions after the function act as test cases before the final print.
```

**Exit code:** 1
**Output:** `  File "/mnt/b6d8b397-9fc1-42ac-a0da-8664a73d4ee9/AIRL/benchmarks/output/python/03.py", line 23
    The function uses the filter-map-reduce pipeline: filter evens, double each, sum. Preconditions validate input types, and the postcondition ensures non-negative results for non-negative inputs. The inline assertions after the function act as test cases before the final print.
        ^^^^^^^^
SyntaxError: invalid syntax`

#### Task 04: Input Validation

```python
def validate_age(age):
    assert isinstance(age, int), "age must be an integer"

    if age < 0:
        result = ("Err", f"Invalid age: {age} is negative")
    elif age > 150:
        result = ("Err", f"Invalid age: {age} exceeds maximum of 150")
    else:
        result = ("Ok", age)

    # Postconditions
    assert isinstance(result, tuple) and len(result) == 2, "result must be a 2-tuple"
    assert result[0] in ("Ok", "Err"), "result tag must be Ok or Err"
    if result[0] == "Ok":
        assert 0 <= result[1] <= 150, "Ok value must be in valid range"
    if result[0] == "Err":
        assert isinstance(result[1], str), "Err value must be a string message"

    return result


# Test cases
result1 = validate_age(25)
assert result1 == ("Ok", 25), f"Expected ('Ok', 25), got {result1}"

result2 = validate_age(-5)
assert result2[0] == "Err", f"Expected Err for -5, got {result2}"

result3 = validate_age(200)
assert result3[0] == "Err", f"Expected Err for 200, got {result3}"

# Boundary checks
assert validate_age(0) == ("Ok", 0), "Age 0 should be valid"
assert validate_age(150) == ("Ok", 150), "Age 150 should be valid"
assert validate_age(151)[0] == "Err", "Age 151 should be invalid"

print(result1)
```

**Exit code:** 0
**Output:** `('Ok', 25)`

#### Task 05: String Tokenizer

```python
def tokenize(s):
    assert isinstance(s, str), "input must be a string"
    result = s.split()
    assert isinstance(result, list), "result must be a list"
    assert all(len(tok) > 0 for tok in result), "all tokens must be non-empty"
    return result


assert tokenize("hello  world  foo") == ["hello", "world", "foo"]
assert tokenize("") == []
assert tokenize("   ") == []

result = tokenize("hello  world  foo")
print(result)
```

**Exit code:** 0
**Output:** `['hello', 'world', 'foo']`

#### Task 06: Absolute Value

```python
def absolute_value(x):
    assert isinstance(x, int), "x must be an integer"

    if x < 0:
        result = -x
    else:
        result = x

    assert result >= 0, "result must be non-negative"
    return result


assert absolute_value(-5) == 5, "absolute_value(-5) should be 5"
assert absolute_value(3) == 3, "absolute_value(3) should be 3"
assert absolute_value(0) == 0, "absolute_value(0) should be 0"

print(absolute_value(-42))
```

**Exit code:** 0
**Output:** `42`

#### Task 07: GCD

```python
def gcd(a, b):
    assert isinstance(a, int), "a must be an integer"
    assert isinstance(b, int), "b must be an integer"
    assert a >= 0, "a must be non-negative"
    assert b >= 0, "b must be non-negative"

    x, y = a, b
    while y != 0:
        x, y = y, x % y

    result = x
    assert result >= 0, "result must be non-negative"
    assert (a == 0 and result == b) or (b == 0 and result == a) or (a % result == 0 and b % result == 0), \
        "result must divide both inputs"
    return result


assert gcd(12, 8) == 4, "gcd(12, 8) should be 4"
assert gcd(15, 0) == 15, "gcd(15, 0) should be 15"

result = gcd(48, 18)
print(result)
```

**Exit code:** 0
**Output:** `6`

#### Task 08: Power

```python
def power(base, exponent):
    assert isinstance(base, int), "base must be an integer"
    assert isinstance(exponent, int), "exponent must be an integer"
    assert exponent >= 0, "exponent must be non-negative"

    result = 1
    for _ in range(exponent):
        result *= base

    assert exponent != 0 or result == 1, "power(x, 0) must return 1"
    if base > 0:
        assert result >= 1, "result must be >= 1 when base > 0"

    return result


# Test cases
result1 = power(2, 10)
assert result1 == 1024, f"Expected 1024, got {result1}"

result2 = power(3, 0)
assert result2 == 1, f"Expected 1, got {result2}"

print(result1)
```

**Exit code:** 0
**Output:** `1024`

#### Task 09: Reverse List

```python
def reverse_list(lst):
    assert isinstance(lst, list), "input must be a list"
    assert all(isinstance(x, int) for x in lst), "all elements must be integers"

    original_length = len(lst)
    original_copy = lst[:]

    result = lst[::-1]

    assert len(result) == original_length, "result length must equal input length"
    assert lst == original_copy, "original list must not be modified"
    if len(lst) == 0:
        assert result == [], "empty list reversed must be empty"

    return result


# Test cases
assert reverse_list([1, 2, 3, 4]) == [4, 3, 2, 1], "test case 1 failed"
assert reverse_list([]) == [], "test case 2 failed"

result = reverse_list([1, 2, 3, 4, 5])
print(result)
```

**Exit code:** 0
**Output:** `[5, 4, 3, 2, 1]`

#### Task 10: Find Max

```python
def find_max(lst):
    assert isinstance(lst, list), "input must be a list"
    assert len(lst) > 0, "input list must not be empty"
    assert all(isinstance(x, int) for x in lst), "all elements must be integers"

    result = lst[0]
    for x in lst[1:]:
        if x > result:
            result = x

    assert result >= max(lst), "result must be >= every element"
    assert result in lst, "result must be an element of the list"
    assert all(result >= x for x in lst), "result must be >= every element in the list"

    return result


# Test cases
assert find_max([3, 1, 4, 1, 5, 9]) == 9, "test case 1 failed"
assert find_max([42]) == 42, "test case 2 failed"

# Required output
print(find_max([3, 7, 2, 8, 1, 9, 4]))

Output: `9`
```

**Exit code:** 1
**Output:** `  File "/mnt/b6d8b397-9fc1-42ac-a0da-8664a73d4ee9/AIRL/benchmarks/output/python/10.py", line 25
    Output: `9`
            ^
SyntaxError: invalid syntax`

#### Task 11: Remove Duplicates

```python
def remove_duplicates(lst):
    # Preconditions
    assert isinstance(lst, list), "input must be a list"
    assert all(isinstance(x, int) for x in lst), "all elements must be integers"

    seen = set()
    result = []
    for x in lst:
        if x not in seen:
            seen.add(x)
            result.append(x)

    # Postconditions
    assert len(result) <= len(lst), "result length must be <= input length"
    assert len(result) == len(set(result)), "each element in result must appear exactly once"
    for i, val in enumerate(result):
        assert val in lst, "result elements must come from input"
        if i > 0:
            first_prev = lst.index(result[i - 1])
            first_curr = lst.index(val)
            assert first_prev < first_curr, "relative order of first occurrences must be preserved"

    return result


assert remove_duplicates([1, 2, 3, 2, 1, 4]) == [1, 2, 3, 4]
assert remove_duplicates([]) == []

print(remove_duplicates([5, 3, 5, 1, 3, 2, 1]))
```

**Exit code:** 0
**Output:** `[5, 3, 1, 2]`

#### Task 12: Zip Lists

```python
def zip_lists(xs, ys):
    assert isinstance(xs, list), "xs must be a list"
    assert isinstance(ys, list), "ys must be a list"

    result = []
    min_len = min(len(xs), len(ys))
    for i in range(min_len):
        result.append((xs[i], ys[i]))

    assert len(result) == min(len(xs), len(ys)), "result length must equal min of input lengths"
    for i in range(len(result)):
        assert result[i] == (xs[i], ys[i]), f"pair at index {i} must match corresponding elements"

    return result


result1 = zip_lists([1, 2, 3], [10, 20, 30])
assert result1 == [(1, 10), (2, 20), (3, 30)], f"expected [(1, 10), (2, 20), (3, 30)], got {result1}"

result2 = zip_lists([1, 2], [10, 20, 30])
assert result2 == [(1, 10), (2, 20)], f"expected [(1, 10), (2, 20)], got {result2}"

print(result1)
```

**Exit code:** 0
**Output:** `[(1, 10), (2, 20), (3, 30)]`

#### Task 13: Palindrome Check

```python
def is_palindrome(s):
    assert isinstance(s, str), "input must be a string"
    result = s == s[::-1]
    assert isinstance(result, bool), "result must be a boolean"
    return result

assert is_palindrome("racecar") == True, "racecar should be a palindrome"
assert is_palindrome("hello") == False, "hello should not be a palindrome"
assert is_palindrome("") == True, "empty string should be a palindrome"
assert is_palindrome("a") == True, "single char should be a palindrome"

print(is_palindrome("racecar"))
```

**Exit code:** 0
**Output:** `True`

#### Task 14: Count Vowels

```python
def count_vowels(s):
    assert isinstance(s, str), "input must be a string"

    result = sum(1 for c in s if c.lower() in "aeiou")

    assert result >= 0, "result must be non-negative"
    assert result <= len(s), "result must not exceed string length"
    return result


assert count_vowels("hello world") == 3
assert count_vowels("xyz") == 0

print(count_vowels("hello world"))
```

**Exit code:** 0
**Output:** `3`

#### Task 15: Caesar Cipher

```python
def caesar_cipher(text, shift):
    assert isinstance(text, str), "text must be a string"
    assert isinstance(shift, int), "shift must be an integer"
    assert 0 <= shift <= 25, "shift must be between 0 and 25 inclusive"

    result = []
    for ch in text:
        if 'a' <= ch <= 'z':
            result.append(chr((ord(ch) - ord('a') + shift) % 26 + ord('a')))
        elif 'A' <= ch <= 'Z':
            result.append(chr((ord(ch) - ord('A') + shift) % 26 + ord('A')))
        else:
            result.append(ch)
    output = ''.join(result)

    assert len(output) == len(text), "result must have the same length as input"
    return output


assert caesar_cipher("abc", 3) == "def", "test case 1 failed"
assert caesar_cipher("xyz", 3) == "abc", "test case 2 failed"

print(caesar_cipher("hello", 3))

Output: `khoor`
```

**Exit code:** 1
**Output:** `  File "/mnt/b6d8b397-9fc1-42ac-a0da-8664a73d4ee9/AIRL/benchmarks/output/python/15.py", line 25
    Output: `khoor`
            ^
SyntaxError: invalid syntax`

#### Task 16: Safe Sqrt

```python
import math


def safe_sqrt(n):
    """Integer square root with Result-style return: ('Ok', value) or ('Err', message)."""
    assert isinstance(n, int), "n must be an integer"

    if n < 0:
        return ("Err", "input must be non-negative")

    result = math.isqrt(n)

    # Postconditions for valid inputs
    assert result >= 0, "result must be non-negative"
    assert result * result <= n, "floor sqrt squared must not exceed input"
    assert (result + 1) * (result + 1) > n, "result must be the floor square root"

    return ("Ok", result)


# Test cases
r1 = safe_sqrt(16)
assert r1 == ("Ok", 4), f"Expected ('Ok', 4), got {r1}"

r2 = safe_sqrt(10)
assert r2 == ("Ok", 3), f"Expected ('Ok', 3), got {r2}"

r3 = safe_sqrt(-1)
assert r3[0] == "Err", f"Expected Err for negative input, got {r3}"

# Required output
result = safe_sqrt(25)
print(result)
```

**Exit code:** 0
**Output:** `('Ok', 5)`

#### Task 17: Parse Int

```python
def parse_int(s):
    assert isinstance(s, str), "input must be a string"
    assert len(s) > 0, "input must be non-empty"

    digits = s
    if s[0] == '-':
        digits = s[1:]
        assert len(digits) > 0, "string with only minus sign is not valid"

    if digits.isdigit():
        result = int(s)
        return ("Ok", result)
    else:
        return ("Err", "not a number")


result = parse_int("42")
assert result == ("Ok", 42), f"Expected Ok(42), got {result}"

result = parse_int("-7")
assert result == ("Ok", -7), f"Expected Ok(-7), got {result}"

result = parse_int("abc")
assert result == ("Err", "not a number"), f"Expected Err, got {result}"

result = parse_int("123")
print(result)
```

**Exit code:** 0
**Output:** `('Ok', 123)`

#### Task 18: Bounded Access

```python
def bounded_access(lst, index):
    assert isinstance(lst, list), "first argument must be a list"
    assert isinstance(index, int), "index must be an integer"

    if 0 <= index < len(lst):
        result = ("Ok", lst[index])
    else:
        result = ("Err", "out of bounds")

    assert isinstance(result, tuple) and len(result) == 2, "result must be a 2-tuple"
    assert result[0] in ("Ok", "Err"), "result tag must be Ok or Err"
    return result


# Test cases
r1 = bounded_access([10, 20, 30], 1)
assert r1 == ("Ok", 20), f"expected Ok(20), got {r1}"

r2 = bounded_access([10, 20, 30], 5)
assert r2 == ("Err", "out of bounds"), f"expected Err('out of bounds'), got {r2}"

# Required output
result = bounded_access([10, 20, 30, 40, 50], 2)
print(result)

Output: `('Ok', 30)`
```

**Exit code:** 1
**Output:** `  File "/mnt/b6d8b397-9fc1-42ac-a0da-8664a73d4ee9/AIRL/benchmarks/output/python/18.py", line 26
    Output: `('Ok', 30)`
            ^
SyntaxError: invalid syntax`

#### Task 19: Flatten List

```python
def flatten(lists):
    assert isinstance(lists, list), "input must be a list"
    for inner in lists:
        assert isinstance(inner, list), "each element must be a list"

    result = [x for inner in lists for x in inner]

    assert isinstance(result, list), "result must be a list"
    assert len(result) == sum(len(inner) for inner in lists), "result length must equal total of inner list lengths"
    return result


assert flatten([[1, 2], [3], [4, 5, 6]]) == [1, 2, 3, 4, 5, 6]
assert flatten([[], [1], []]) == [1]

print(flatten([[1, 2], [3, 4], [5]]))

Output: `[1, 2, 3, 4, 5]`
```

**Exit code:** 1
**Output:** `  File "/mnt/b6d8b397-9fc1-42ac-a0da-8664a73d4ee9/AIRL/benchmarks/output/python/19.py", line 18
    Output: `[1, 2, 3, 4, 5]`
            ^
SyntaxError: invalid syntax`

#### Task 20: Group by Parity

```python
def group_by_parity(nums):
    assert isinstance(nums, list), "input must be a list"
    assert all(isinstance(x, int) for x in nums), "all elements must be integers"

    evens = [x for x in nums if x % 2 == 0]
    odds = [x for x in nums if x % 2 != 0]

    assert len(evens) + len(odds) == len(nums), "all elements must be accounted for"
    assert all(x % 2 == 0 for x in evens), "evens list must contain only even numbers"
    assert all(x % 2 != 0 for x in odds), "odds list must contain only odd numbers"
    assert evens + odds == [x for x in nums if x % 2 == 0] + [x for x in nums if x % 2 != 0], "relative order must be preserved"

    return (evens, odds)


result = group_by_parity([1, 2, 3, 4, 5, 6])
assert result == ([2, 4, 6], [1, 3, 5]), f"expected ([2, 4, 6], [1, 3, 5]), got {result}"

result = group_by_parity([])
assert result == ([], []), f"expected ([], []), got {result}"

result = group_by_parity([10, 15, 20, 25, 30])
print(result)
```

**Exit code:** 0
**Output:** `([10, 20, 30], [15, 25])`

#### Task 21: Running Sum

```python
def running_sum(nums):
    assert isinstance(nums, list), "input must be a list"
    assert all(isinstance(x, int) for x in nums), "all elements must be integers"

    result = []
    acc = 0
    for x in nums:
        acc += x
        result.append(acc)

    assert len(result) == len(nums), "result length must equal input length"
    for i in range(len(result)):
        assert result[i] == sum(nums[:i + 1]), f"element {i} must equal sum of inputs 0..{i}"

    return result


# Test cases
assert running_sum([1, 2, 3, 4]) == [1, 3, 6, 10], "test case 1 failed"
assert running_sum([]) == [], "test case 2 failed"

# Required output
print(running_sum([1, 2, 3, 4, 5]))
```

**Exit code:** 0
**Output:** `[1, 3, 6, 10, 15]`

#### Task 22: Word Frequency

```python
def word_frequency(s):
    assert isinstance(s, str), "input must be a string"

    words = s.split()
    counts = {}
    order = []

    for word in words:
        if word not in counts:
            counts[word] = 0
            order.append(word)
        counts[word] += 1

    result = [[word, counts[word]] for word in order]

    # Postconditions
    assert len(result) == len(set(words)), "each word must appear exactly once"
    assert sum(count for _, count in result) == len(words), "total counts must equal total words"
    for word, count in result:
        assert count > 0, f"count for '{word}' must be positive"

    return result


# Test case from spec
test_result = word_frequency("the cat and the dog")
assert test_result == [["the", 2], ["cat", 1], ["and", 1], ["dog", 1]], f"test failed: {test_result}"

# Required output
print(word_frequency("one two one three two one"))
```

**Exit code:** 0
**Output:** `[['one', 3], ['two', 2], ['three', 1]]`

#### Task 23: Matrix Transpose

```python
def transpose(matrix):
    # Preconditions
    assert isinstance(matrix, list), "Input must be a list"
    assert len(matrix) > 0, "Matrix must be non-empty"
    assert all(isinstance(row, list) for row in matrix), "All elements must be lists"
    row_len = len(matrix[0])
    assert row_len > 0, "Rows must be non-empty"
    assert all(len(row) == row_len for row in matrix), "All rows must have the same length"

    m = len(matrix)
    n = row_len
    result = [[matrix[i][j] for i in range(m)] for j in range(n)]

    # Postconditions
    assert len(result) == n, "Result must have n rows"
    assert all(len(row) == m for row in result), "Result rows must have m columns"
    assert all(result[j][i] == matrix[i][j] for i in range(m) for j in range(n)), "Transpose property: result[j][i] == matrix[i][j]"

    return result


# Test case from requirements
assert transpose([[1, 2, 3], [4, 5, 6]]) == [[1, 4], [2, 5], [3, 6]]

# Print result for specified input
result = transpose([[1, 2], [3, 4], [5, 6]])
print(result)

Expected output: `[[1, 3, 5], [2, 4, 6]]`
```

**Exit code:** 1
**Output:** `  File "/mnt/b6d8b397-9fc1-42ac-a0da-8664a73d4ee9/AIRL/benchmarks/output/python/23.py", line 29
    Expected output: `[[1, 3, 5], [2, 4, 6]]`
             ^^^^^^
SyntaxError: invalid syntax`

#### Task 24: Merge Sorted

```python
def merge_sorted(a, b):
    # Preconditions
    assert isinstance(a, list), "a must be a list"
    assert isinstance(b, list), "b must be a list"
    assert all(isinstance(x, int) for x in a), "a must contain only integers"
    assert all(isinstance(x, int) for x in b), "b must contain only integers"
    assert a == sorted(a), "a must be sorted in ascending order"
    assert b == sorted(b), "b must be sorted in ascending order"

    result = []
    i, j = 0, 0
    while i < len(a) and j < len(b):
        if a[i] <= b[j]:
            result.append(a[i])
            i += 1
        else:
            result.append(b[j])
            j += 1
    result.extend(a[i:])
    result.extend(b[j:])

    # Postconditions
    assert len(result) == len(a) + len(b), "result length must equal sum of input lengths"
    assert result == sorted(result), "result must be sorted in ascending order"

    return result


# Test cases
result1 = merge_sorted([1, 3, 5], [2, 4, 6])
assert result1 == [1, 2, 3, 4, 5, 6], f"Expected [1, 2, 3, 4, 5, 6], got {result1}"

result2 = merge_sorted([], [1, 2])
assert result2 == [1, 2], f"Expected [1, 2], got {result2}"

# Final output
result = merge_sorted([1, 3, 5, 7], [2, 4, 6, 8])
print(result)

Output: `[1, 2, 3, 4, 5, 6, 7, 8]`
```

**Exit code:** 1
**Output:** `  File "/mnt/b6d8b397-9fc1-42ac-a0da-8664a73d4ee9/AIRL/benchmarks/output/python/24.py", line 40
    Output: `[1, 2, 3, 4, 5, 6, 7, 8]`
            ^
SyntaxError: invalid syntax`

#### Task 25: Pipeline

```python
def positive_square_sum(nums):
    assert isinstance(nums, list), "input must be a list"
    assert all(isinstance(x, int) for x in nums), "all elements must be integers"

    result = sum(x * x for x in nums if x > 0)

    assert result >= 0, "result must be non-negative"
    return result


# Test cases
assert positive_square_sum([1, -2, 3, -4, 5]) == 35, "expected 35"
assert positive_square_sum([-1, -2]) == 0, "expected 0"
assert positive_square_sum([]) == 0, "empty list should return 0"

# Required output
print(positive_square_sum([3, -1, 4, -1, 5, -9, 2]))

The output will be **54** (9 + 16 + 25 + 4 = 54).
```

**Exit code:** 1
**Output:** `  File "/mnt/b6d8b397-9fc1-42ac-a0da-8664a73d4ee9/AIRL/benchmarks/output/python/25.py", line 19
    The output will be **54** (9 + 16 + 25 + 4 = 54).
        ^^^^^^
SyntaxError: invalid syntax`

