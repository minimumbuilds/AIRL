;; ERROR: borrowed
;; Cannot move a value that is also passed as a borrow in the same call
(defn both-ops
  :sig [(ref a : i32) (own b : i32) -> i32]
  :intent "test borrow and move"
  :requires [(valid a) (valid b)]
  :ensures [(valid result)]
  :body (+ a b))
(let (v : i32 42) (both-ops v v))
