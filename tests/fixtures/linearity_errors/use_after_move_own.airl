;; ERROR: moved
;; Passing with own should move the value
(defn consume
  :sig [(own x : i32) -> i32]
  :intent "consume"
  :requires [(valid x)]
  :ensures [(valid result)]
  :body x)
(let (v : i32 42) (do (consume v) v))
