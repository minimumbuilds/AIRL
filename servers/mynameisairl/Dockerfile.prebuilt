FROM ubuntu:24.04
COPY mynameisairl /usr/local/bin/mynameisairl
COPY mcp-stdio-wrapper.sh /usr/local/bin/mcp-stdio-wrapper.sh
# Guide is now embedded in the binary at build time — no file mount needed
ENTRYPOINT ["/usr/local/bin/mcp-stdio-wrapper.sh"]
